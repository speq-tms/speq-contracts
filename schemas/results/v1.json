{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://speq.dev/schemas/results/v1.json",
  "title": "speq run result v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "startedAtMs", "durationMs", "totals", "tests"],
  "description": "speq CLI run summary output. Backward-compatible: consumers should accept unknown fields.",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["passed", "failed", "pending"]
    },
    "startedAtMs": {
      "type": "integer",
      "minimum": 0
    },
    "durationMs": {
      "type": "integer",
      "minimum": 0
    },
    "totals": {
      "type": "object",
      "additionalProperties": false,
      "required": ["passed", "failed", "total"],
      "properties": {
        "passed": {
          "type": "integer",
          "minimum": 0
        },
        "failed": {
          "type": "integer",
          "minimum": 0
        },
        "total": {
          "type": "integer",
          "minimum": 0
        },
        "pending": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of tests/steps skipped due to ATDD pending status."
        },
        "error": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of tests that encountered runtime errors."
        }
      }
    },
    "tests": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "status", "durationMs"],
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1
          },
          "status": {
            "type": "string",
            "enum": ["passed", "failed", "pending", "error"]
          },
          "message": {
            "type": "string"
          },
          "durationMs": {
            "type": "integer",
            "minimum": 0
          }
        }
      }
    }
  }
}
